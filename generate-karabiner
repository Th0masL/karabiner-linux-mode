#!/usr/bin/env python3
"""Run the Karabiner generator from its organized scripts directory."""

from pathlib import Path
import runpy


runpy.run_path(
    str(Path(__file__).resolve().parent / "scripts/generate-karabiner"),
    run_name="__main__",
)
